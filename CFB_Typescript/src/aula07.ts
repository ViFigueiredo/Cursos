/* Tuplas */

// exemplo de union types
let coisas: (number | string)[] = [10, 20, 30, 'Teste'];

// exemplo de tuplas
let inventario: [string, number, number] = ['corda', 10, 20]
console.log(inventario);
